version: "3.7"

services:
  # app:
  #   build:
  #     context: ../
  #     dockerfile: Dockerfile
  #   image: dcard-app
  #   container_name: dcard-app
  #   restart: unless-stopped
  #   ports:
  #     - '9000:5000'
  #   volumes:
  #     - ../app:/code
  #   networks:
  #     - dcard-networks
  #   depends_on:
  #     - db

#  db:
#    image: postgres:latest
#    container_name: dcard-db
#    restart: always
#    environment:
#      POSTGRES_DATABASE: ${APP_DB_DATABASE}
#      POSTGRES_USER: ${APP_DB_USERNAME}
#      POSTGRES_PASSWORD: ${APP_DB_PASSWORD}
#    ports:
#      - "5432:5432"
#    networks:
#      - dcard-networks

  pgadmin:
    image: dpage/pgadmin4
    container_name: dcard-pgadmin
    restart: unless-stopped
    ports:
      - '8085:80'
    environment:
      PGADMIN_DEFAULT_EMAIL: ${APP_PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${APP_PGADMIN_PASSWORD}
    networks:
      - dcard-networks

#  redis:
#    image: redis:latest
#    restart: always
#    environment:
#      TZ: ${APP_SERVER_TIMEZONE}
#    expose:
#      - ${APP_REDIS_PORT}

networks:
  dcard-networks:
    driver: bridge

#volumes:
#  dcard-pgadmin:
#    name: dcard-pgadmin